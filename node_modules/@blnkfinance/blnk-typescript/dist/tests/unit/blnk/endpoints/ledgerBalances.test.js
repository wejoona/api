"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
/* eslint-disable n/no-unpublished-import */
const tap_1 = __importDefault(require("tap"));
const ledgerBalances_1 = require("../../../../src/blnk/endpoints/ledgerBalances");
const blnkClientMocks_1 = require("../../../mocks/blnkClientMocks");
const httpClient_1 = require("../../../../src/blnk/utils/httpClient");
tap_1.default.test(`Ledger Balance Tests`, t => {
    const mockLogger = (0, blnkClientMocks_1.createMockLogger)();
    t.test(`it should create a ledger balance when valid data is provided`, (tt) => __awaiter(void 0, void 0, void 0, function* () {
        var _a;
        const thirdPartyRequest = (0, blnkClientMocks_1.createMockBlnkRequest)(true, undefined, 201);
        const capturedRequest = tt.captureFn(thirdPartyRequest);
        const ledgerBalance = new ledgerBalances_1.LedgerBalances(capturedRequest, mockLogger, httpClient_1.FormatResponse);
        const data = {
            currency: `USD`,
            ledger_id: blnkClientMocks_1.ledgerId,
            meta_data: {
                company_name: `Test Company`,
            },
        };
        const response = yield ledgerBalance.create(data);
        //verify that the request function was called and with the right parameters
        tt.match(capturedRequest.args(), [
            [
                `balances`,
                {
                    currency: `USD`,
                    ledger_id: blnkClientMocks_1.ledgerId,
                    meta_data: {
                        company_name: `Test Company`,
                    },
                },
                `POST`,
            ],
        ]);
        tt.equal(response.status, 201, `Response is 201`);
        tt.equal((_a = response.data) === null || _a === void 0 ? void 0 : _a.ledger_id, blnkClientMocks_1.ledgerId);
        tt.end();
    }));
    t.test(`it should handle missing optional fields`, (tt) => __awaiter(void 0, void 0, void 0, function* () {
        var _a;
        const thirdPartyRequest = (0, blnkClientMocks_1.createMockBlnkRequest)(true, undefined, 201);
        const capturedRequest = tt.captureFn(thirdPartyRequest);
        const ledgerBalance = new ledgerBalances_1.LedgerBalances(capturedRequest, mockLogger, httpClient_1.FormatResponse);
        const data = {
            currency: `USD`,
            ledger_id: blnkClientMocks_1.ledgerId,
            meta_data: {
                company_name: `Test Company`,
            },
        };
        const response = yield ledgerBalance.create(data);
        //verify that the request function was called and with the right parameters
        tt.match(capturedRequest.args(), [
            [
                `balances`,
                {
                    currency: `USD`,
                    ledger_id: `123456`,
                    meta_data: {
                        company_name: `Test Company`,
                    },
                },
                `POST`,
            ],
        ]);
        tt.equal(response.status, 201, `Response is 200`);
        tt.equal((_a = response.data) === null || _a === void 0 ? void 0 : _a.identity_id, undefined);
        tt.end();
    }));
    t.test(`it should handle missing required fields`, (tt) => __awaiter(void 0, void 0, void 0, function* () {
        const thirdPartyRequest = (0, blnkClientMocks_1.createMockBlnkRequest)(true);
        const capturedRequest = tt.captureFn(thirdPartyRequest);
        const ledgerBalance = new ledgerBalances_1.LedgerBalances(capturedRequest, mockLogger, httpClient_1.FormatResponse);
        //using any here to simulate a developer passing in any and missing required data types
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const data = {
            currency: `USD`,
            meta_data: {
                company_name: `Test Company`,
            },
        };
        const response = yield ledgerBalance.create(data);
        //verify that the request function was called and with the right parameters
        //request won't get called since this fails validation, so we use empty array to compare the captured args
        tt.match(capturedRequest.args(), []);
        tt.equal(response.status, 400, `Response is 500`);
        tt.equal(response.data, null);
        tt.end();
    }));
    t.test(`it should handle thrown during balance creation`, (tt) => __awaiter(void 0, void 0, void 0, function* () {
        const thirdPartyRequest = (0, blnkClientMocks_1.createMockBlnkRequest)(true, `Network Error`);
        const capturedRequest = tt.captureFn(thirdPartyRequest);
        const ledgerBalance = new ledgerBalances_1.LedgerBalances(capturedRequest, mockLogger, httpClient_1.FormatResponse);
        const data = {
            currency: `USD`,
            ledger_id: blnkClientMocks_1.ledgerId,
            meta_data: {
                company_name: `Test Company`,
            },
        };
        const response = yield ledgerBalance.create(data);
        //verify that the request function was called and with the right parameters
        tt.match(capturedRequest.args(), [[`balances`, data, `POST`]]);
        tt.equal(response.status, 500, `Response is 500`);
        tt.equal(response.data, null);
        tt.equal(response.message, `Network Error`);
        tt.end();
    }));
    t.test(`it should handle meta_data if it is not an object`, (tt) => __awaiter(void 0, void 0, void 0, function* () {
        const thirdPartyRequest = (0, blnkClientMocks_1.createMockBlnkRequest)(true);
        const capturedRequest = tt.captureFn(thirdPartyRequest);
        const ledgerBalance = new ledgerBalances_1.LedgerBalances(capturedRequest, mockLogger, httpClient_1.FormatResponse);
        //allow any for this line, so we can test what happens if a dev passes in a data type for meta_data that doesnt meet the requirements for Record<string, unknown>
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const data = {
            currency: `USD`,
            ledger_id: blnkClientMocks_1.ledgerId,
            meta_data: 5,
        };
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const response = yield ledgerBalance.create(data);
        //verify that the request function was called and with the right parameters
        tt.match(capturedRequest.args(), []);
        tt.equal(response.status, 400, `Response is 400`);
        tt.equal(response.data, null);
        tt.equal(response.message, `meta_data must be a valid object if provided`);
        tt.end();
    }));
    t.end();
});
//# sourceMappingURL=ledgerBalances.test.js.map