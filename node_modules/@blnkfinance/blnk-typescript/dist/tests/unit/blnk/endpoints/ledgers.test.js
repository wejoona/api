"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
/* eslint-disable n/no-unpublished-import */
const tap_1 = __importDefault(require("tap"));
const blnkClientMocks_1 = require("../../../mocks/blnkClientMocks");
const ledgers_1 = require("../../../../src/blnk/endpoints/ledgers");
const httpClient_1 = require("../../../../src/blnk/utils/httpClient");
tap_1.default.test(`Ledger Tests`, (t) => __awaiter(void 0, void 0, void 0, function* () {
    const mockLogger = (0, blnkClientMocks_1.createMockLogger)();
    let thirdPartyRequest;
    t.beforeEach(() => {
        thirdPartyRequest = (0, blnkClientMocks_1.createMockBlnkRequest)(true, undefined, 201);
    });
    t.test(`Creates a ledger with valid data`, (tt) => __awaiter(void 0, void 0, void 0, function* () {
        var _a;
        const capturedRequest = t.captureFn(thirdPartyRequest);
        const ledger = new ledgers_1.Ledgers(capturedRequest, mockLogger, httpClient_1.FormatResponse);
        const data = {
            name: `My Ledger`,
            meta_data: {
                company_name: `Test Company`,
            },
        };
        const response = yield ledger.create(data);
        tt.match(capturedRequest.args(), [[`ledgers`, data, `POST`]]);
        tt.equal(response.status, 201);
        tt.equal((_a = response.data) === null || _a === void 0 ? void 0 : _a.name, data.name);
    }));
    t.test(`it should handle missing required fields`, (tt) => __awaiter(void 0, void 0, void 0, function* () {
        const thirdPartyRequest = (0, blnkClientMocks_1.createMockBlnkRequest)(true);
        const capturedRequest = tt.captureFn(thirdPartyRequest);
        const ledgers = new ledgers_1.Ledgers(capturedRequest, mockLogger, httpClient_1.FormatResponse);
        //using any here to simulate a developer passing in any and missing required data types
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const data = {
            meta_data: {
                company_name: `Test Company`,
            },
        };
        const response = yield ledgers.create(data);
        //verify that the request function was called and with the right parameters
        //request won't get called since this fails validation, so we use empty array to compare the captured args
        tt.match(capturedRequest.args(), []);
        tt.equal(response.status, 400, `Response is 400`);
        tt.equal(response.data, null);
    }));
    t.test(`it should handle thrown errors gracefully`, (tt) => __awaiter(void 0, void 0, void 0, function* () {
        const thirdPartyRequest = (0, blnkClientMocks_1.createMockBlnkRequest)(true, `Network Error`);
        const capturedRequest = tt.captureFn(thirdPartyRequest);
        const ledgerBalance = new ledgers_1.Ledgers(capturedRequest, mockLogger, httpClient_1.FormatResponse);
        const data = {
            name: `Test Ledger`,
            meta_data: {
                company_name: `Test Company`,
            },
        };
        const response = yield ledgerBalance.create(data);
        //verify that the request function was called and with the right parameters
        tt.match(capturedRequest.args(), [[`ledgers`, data, `POST`]]);
        tt.equal(response.status, 500, `Response is 500`);
        tt.equal(response.data, null);
        tt.equal(response.message, `Network Error`);
    }));
}));
//# sourceMappingURL=ledgers.test.js.map