"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isValidMetaData = void 0;
exports.ValidateCreateLedgerBalance = ValidateCreateLedgerBalance;
const stringUtils_1 = require("../stringUtils");
function ValidateCreateLedgerBalance(data) {
    // Validate if data is an object
    if (!data || typeof data !== `object`) {
        return `Data must be a valid object of type CreateLedgerBalance`;
    }
    // Validate ledger_id
    if (!(0, stringUtils_1.IsValidString)(data.ledger_id)) {
        return `ledger_id must be a valid string`;
    }
    // Validate identity_id if provided
    if (data.identity_id !== undefined && !(0, stringUtils_1.IsValidString)(data.identity_id)) {
        return `identity_id must be a valid string if provided`;
    }
    // Validate currency
    if (!(0, stringUtils_1.IsValidString)(data.currency)) {
        return `currency must be either 'USD' or 'NGN'`;
    }
    // Validate meta_data if provided
    if (data.meta_data !== undefined && !(0, exports.isValidMetaData)(data.meta_data)) {
        return `meta_data must be a valid object if provided`;
    }
    // If all validations pass, return null
    return null;
}
const isValidMetaData = (meta) => typeof meta === `object` && meta !== null;
exports.isValidMetaData = isValidMetaData;
//# sourceMappingURL=ledgerBalance.js.map