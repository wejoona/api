"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MapResponse = MapResponse;
exports.FormatResponse = FormatResponse;
const stringUtils_1 = require("./stringUtils");
/**
 * Maps the response object to a new object based on the specified fields to keep.
 *
 * @param response The input response object to map.
 * @param keepFields An array of keys representing the fields to keep in the mapped object.
 * @returns The mapped object with only the specified fields.
 */
function MapResponse(response, keepFields) {
    const result = {}; // Use a generic object first
    Object.keys(response).forEach(key => {
        const newKey = keepFields.includes(key)
            ? key
            : (0, stringUtils_1.ToCamelCase)(key);
        result[newKey] = response[key];
    });
    return result; // Cast it to TOutput at the end
}
function FormatResponse(status, message, data) {
    return { status, message, data };
}
//# sourceMappingURL=httpClient.js.map