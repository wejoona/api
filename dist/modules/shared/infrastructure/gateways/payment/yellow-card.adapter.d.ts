import { ConfigService } from '@nestjs/config';
import { IPaymentGateway, CreateSubwalletRequest, Subwallet, BalanceResponse, OnRampChannel, InitiateDepositRequest, DepositResponse, InternalTransferRequest, ExternalTransferRequest, TransferResponse, RateRequest, RateResponse, SubmitKycRequest, KycResponse, WebhookEvent } from '../../../domain/gateways/payment.gateway';
export declare class YellowCardPaymentAdapter implements IPaymentGateway {
    private readonly configService;
    private readonly logger;
    private readonly config;
    readonly providerName = "yellow_card";
    constructor(configService: ConfigService);
    createSubwallet(request: CreateSubwalletRequest): Promise<Subwallet>;
    getBalance(subwalletId: string): Promise<BalanceResponse>;
    getOnRampChannels(country: string, currency?: string): Promise<OnRampChannel[]>;
    initiateDeposit(request: InitiateDepositRequest): Promise<DepositResponse>;
    getDepositStatus(depositId: string): Promise<DepositResponse>;
    internalTransfer(request: InternalTransferRequest): Promise<TransferResponse>;
    externalTransfer(request: ExternalTransferRequest): Promise<TransferResponse>;
    getTransferStatus(transferId: string): Promise<TransferResponse>;
    getRate(request: RateRequest): Promise<RateResponse>;
    submitKyc(request: SubmitKycRequest): Promise<KycResponse>;
    getKycStatus(subwalletId: string): Promise<KycResponse>;
    verifyWebhookSignature(payload: string, signature: string): boolean;
    parseWebhookEvent(payload: Record<string, unknown>): WebhookEvent;
    private mapWebhookEventType;
    private mockCreateSubwallet;
    private mockGetBalance;
    private mockGetOnRampChannels;
    private mockInitiateDeposit;
    private mockGetDepositStatus;
    private mockInternalTransfer;
    private mockExternalTransfer;
    private mockGetTransferStatus;
    private mockGetRate;
    private mockSubmitKyc;
    private mockGetKycStatus;
    private generateMockAddress;
    private apiCreateSubwallet;
    private apiGetBalance;
    private apiGetOnRampChannels;
    private apiInitiateDeposit;
    private apiGetDepositStatus;
    private apiInternalTransfer;
    private apiExternalTransfer;
    private apiGetTransferStatus;
    private apiGetRate;
    private apiSubmitKyc;
    private apiGetKycStatus;
}
