apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secrets.yaml

images:
  - name: registry.wejoona.com/usdc-wallet-api
    newTag: latest
