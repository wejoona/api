apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - minio.yaml
  # secrets.yaml managed separately (not in git)

images:
  - name: registry.wejoona.com/usdc-wallet-api
    newTag: 3120ac00ea46bdbda80069efb6a0905a88efc570
