apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - minio.yaml
  # secrets.yaml managed separately (not in git)

images:
  - name: registry.wejoona.com/usdc-wallet-api
    newTag: 12e36a0f3553a0d29248f63f2d65b483bfaa3eac
